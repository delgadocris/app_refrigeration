<% provide(:title, t('accions.show', attribute: t('branch.one'))) %>
<div class="mx-auto md:w-2/3 w-full">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="alert alert-success" id="notice"><%= notice %></p>
    <% end %>
    <br><br>
    <h1 class="font-bold text-4xl"><%= t('accions.show', attribute: t('branch.one')) %></h1>

    <%= render @branch %>
    <%= link_to t('common.back'), branches_path, class: "btn btn-warning"%></td>
  </div>
</div>
